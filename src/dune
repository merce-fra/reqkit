(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags --table --explain )
)

(library
  (name src)
  (libraries menhirLib)
  (inline_tests)
  (preprocess
    (pps ppx_inline_test bisect_ppx --conditional))
  (instrumentation (backend bisect_ppx))
)

