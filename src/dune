(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags --table --explain )
)

(library
  (name src)
  (modules_without_implementation util)
   (instrumentation (backend bisect_ppx))
  (libraries menhirLib)
)

